// Shimmer for provider profile Details compoenent interface
export interface ShimmerProfileDetailsProps {
    row: number;
}

// Shimmer for provider availability compoenent interface
export interface ShimmerProviderAvailabilityProps {
    slotCount: number;
}