export type Provider = {
    _id: string
    isVerified: boolean
    isBlocked: boolean
    username: string
    email: string
}

export type User = {
    _id: string
    isVerified: boolean
    isBlocked: boolean
    username: string
    email: string
}